# Server port (Railway provides this automatically via PORT env var)
# Do NOT set this on Railway - it's auto-configured
PORT=3000

# Secret passphrase for DM authentication
# REQUIRED: Set this to a secure random string on Railway
DM_SECRET=your-secret-passphrase-here

# ============================================
# RAILWAY DEPLOYMENT INSTRUCTIONS
# ============================================
# 1. Push this repo to GitHub
# 2. Create a new project on Railway
# 3. Connect your GitHub repo
# 4. Add the DM_SECRET environment variable in Railway dashboard
# 5. Railway will auto-detect Node.js and run:
#    - npm install
#    - npm run build
#    - npm start
# 6. Your app will be available at the Railway-provided URL
