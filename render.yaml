services:
  - type: web
    name: dice-temple
    runtime: node
    buildCommand: npm install && npx vite build
    startCommand: npm start
    envVars:
      - key: DM_SECRET
        sync: false
    healthCheckPath: /health
